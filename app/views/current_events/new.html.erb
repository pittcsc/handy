<html>
<head>
	<title>Set the current event</title>
</head>
<body>
<h1>Set current event</h1>
<%= form_for @event, url: {action: "create"} do |event_form| %>
<%= select_tag(:id, options_for_select(@event_list.collect {|a| [a.name, a.id]})) %>
<%= event_form.submit :submit %>
<% end %>
</body>
</html
