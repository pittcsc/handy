<h1>My Organizations</h1>

<ul>
  <% @organizations.each do |organization| %>
    <li><%= link_to organization.name, organization_events_url(organization) %></li>
  <% end %>
</ul>

<%= link_to "New Organization", new_organization_url %>
